# Potrzebne TYLKO gdy frontend hostowany osobno (nie przez backend Express).
# Gdy backend serwuje frontend (Railway — jeden serwis), zostaw puste.
VITE_API_URL=
# Przykład dla osobnego hostingu:
# VITE_API_URL=https://twoja-nazwa.up.railway.app
